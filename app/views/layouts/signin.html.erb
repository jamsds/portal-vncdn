<!DOCTYPE html>
<html>
  <head>
    <% @parent_uuid = User.find_by(domain: request.host) %>
    <meta charset="utf-8">
    <title>Portal :: <% if @parent_uuid.present? && @parent_uuid.company.present? %><%= @parent_uuid.company %><% else %>Vncdn<% end %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
 	<body>
    <div class="main__signin">
      <%= yield %>
    </div>
 	</body>
</html>